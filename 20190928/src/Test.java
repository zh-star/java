public class Test {
    public static void main(String[] args){
        LinkedList node = new LinkedList();
        ListNode head = null;
        node.addLast(head,1);
        node.addLast(head,2);
        node.addLast(head,3);
        node.addLast(head,4);

        node.removeElements()
}
